<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- For Clients -->
    <item
        android:id="@+id/menu_add_job"
        android:title="Add New Job"
        android:visible="false" />

    <item
        android:id="@+id/menu_manage_jobs"
        android:title="Manage My Jobs"
        android:visible="false" />
    <item
        android:id="@+id/menu_view_freelancer_proposals"
        android:title="View your Proposals"
        android:visible="false"
        />

    <!-- For Everyone -->
    <item
        android:id="@+id/menu_update"
        android:title="Profile" />
    <item
        android:id="@+id/menu_logout"
        android:title="Logout" />
    <item
        android:id="@+id/menu_browse"
        android:title="Browse Jobs"
        android:visible="false"
        app:showAsAction="ifRoom" />
    <item
        android:id="@+id/menu_view_proposals"
        android:title="View Proposals"
        android:visible="false"
        app:showAsAction="ifRoom" />

</menu>
